export default function PDFExporter({ labels, values }) {
  return (
    <button
      className="mt-4 bg-gray-800 text-white px-4 py-2 rounded-lg"
      onClick={() => alert("PDF Export Coming soon")}
    >
      📄 Download PDF
    </button>
  );
}